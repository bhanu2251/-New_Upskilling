# ============================================================
# The output have been generated with the assistance of Claude at 2026-06-18T09:52:56Z UTC.
# The content has been verified by the designated engineer.
# ============================================================

name: 'new_upskilling'
version: '1.0.0'
config-version: 2

profile: 'default'

model-paths:    ["models"]
macro-paths:    ["macros"]
test-paths:     ["tests"]
seed-paths:     ["seeds"]
snapshot-paths: ["snapshots"]
analysis-paths: ["analyses"]
target-path:    "target"
clean-targets:  ["target", "dbt_packages"]

vars:
  silver_database:  'BUI_BHANU_RAMIREDDY_DB'
  bronze_database:  'BUI_BHANU_RAMIREDDY_DB'
  bronze_schema:    'RAW'
  environment:      'dev'
  default_timezone: 'UTC'
  gold_database: 'BUI_BHANU_RAMIREDDY_DB'
  gold_schema:   'GOLD'

models:
  new_upskilling:
    staging:
      +materialized: view
      +schema: RAW
      +tags: ["silver", "staging"]
      +on_schema_change: fail

    silver:
      +materialized: table
      +schema: SILVER
      +tags: ["silver"]
      +on_schema_change: fail

    gold:
       +materialized: table
       +schema: GOLD
       +tags: ["gold"]
       +on_schema_change: append_new_columns

       dim:
         +tags: ["gold", "dimension"]

       facts:
         +tags: ["gold", "fact"]
         +materialized: incremental
         +incremental_strategy: merge

data_tests:
  +store_failures: true
  +schema: TEST_FAILURES

quoting:
  database:   true
  schema:     true
  identifier: true

snapshots:
  new_upskilling:
    +target_schema: GOLD
    +tags: ["gold", "scd2"]
