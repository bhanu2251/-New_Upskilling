# Silver Layer — Design Decisions

Generated by silver-layer-scripts-generator-v1 at 2026-06-18T09:52:56Z UTC.

## Phase 0 — Project Inputs

| Decision | Value |
|---|---|
| Platform | Snowflake |
| Framework | dbt Core |
| dbt project name | new_upskilling |
| dbt profile | default |
| Target Silver database | BUI_BHANU_RAMIREDDY_DB |
| Target Silver schema | SILVER |
| Bronze database | BUI_BHANU_RAMIREDDY_DB |
| Bronze schema | RAW |
| Environment | dev |
| Threads | 4 |

## Phase 0E/0F — Model Design

| Decision | Value |
|---|---|
| Folder structure | staging/ + silver/ + gold/ (matches existing project) |
| Silver materialization | table (full, schema: SILVER) |
| Staging materialization | view (schema: RAW) |
| Schema style | Static — explicit column list per model (Option A) |
| Cleaning strategy | Inline SQL (Option A) — LLD TransformationRule expressions |
| Incremental strategy | table (full refresh) — matching existing project pattern |
| Model naming | lowercase snake_case matching existing models |
| CTE chain | source → cleaned → renamed → final (all 15 models) |

## Phase 1 — Standards

| Decision | Value |
|---|---|
| Reserved word handling | 112 Bronze columns double-quoted in renamed CTE |
| Watermark column | _FIVETRAN_SYNCED for 14 tables; TRANSACTIONSTATUS = no watermark |
| DEDUP-1 pattern | QUALIFY ROW_NUMBER() OVER (PARTITION BY pk ORDER BY _FIVETRAN_SYNCED DESC) = 1 |
| Audit col — SILVER_CREATED_ON_TS_UTC | INSERT-only: COALESCE(MIN(existing), CURRENT_TIMESTAMP()) |
| Audit col — SILVER_UPDATED_ON_TS_UTC | Every write: CURRENT_TIMESTAMP() |
| Audit col — SILVER_DELETED_ON_TS_UTC | CAST(NULL AS TIMESTAMP_NTZ) until soft-delete |
| SURROGATE_KEY | MD5(CAST(pk AS VARCHAR)) — sole IsPrimaryKey=YES on every table |

## Silver-layer JOIN decision (Option A — enriched style)

transactionaccountingline joins:
- silver.transaction (void/posting filter + header context)
- silver.transactionline (department, class, location, item context)
- silver.account (account classification + FX rate type)
- silver.consolidatedexchangerate (FX rates for USD translation)

Rationale: matches existing project structure; enriched GL is immediately usable
for Gold FACT_GLJournal without further joins.

## Known Items for Engineer Review

- profiles.yml is a template only — fill DBT_SF_* environment variables before running
- TRANSACTIONSTATUS has no watermark column — always full refresh
- TOSUBSIDIARY used as SUBSIDIARY_ID on TRANSACTION (not t.subsidiary) — confirm with data
- FX rate defaults to 1 when no rate found in CONSOLIDATEDEXCHANGERATE (USD→USD rows)
