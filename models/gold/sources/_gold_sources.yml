# ============================================================
# Gold Layer — Silver Source Declarations
# Generated by gold-script-generator at 2026-06-18T UTC
# Project : new_upskilling | Platform: Snowflake | Framework: dbt Core
# ============================================================
version: 2

sources:
  - name: silver
    database: "{{ var('silver_database') }}"
    schema:   SILVER
    description: >
      Silver layer — cleaned and conformed NetSuite data.
      All 15 source tables land here after Bronze → Silver transformation.
      Gold models reference Silver exclusively via this source block.
    tables:
      - name: account
        description: "Cleaned Chart of Accounts — FINANCIAL_STATEMENT, PL_CATEGORY, BS_CATEGORY derived."
      - name: accountingperiod
        description: "Cleaned fiscal calendar — ISPOSTING and ISCLOSED filters applied in Silver."
      - name: classification
        description: "Cleaned business line / class segments."
      - name: consolidatedexchangerate
        description: "Period-level FX rates — AVERAGE_RATE, CURRENT_RATE, HISTORICAL_RATE."
      - name: currency
        description: "Currency master — IS_BASE_CURRENCY identifies USD as group reporting currency."
      - name: department
        description: "Department hierarchy — cost centre segmentation."
      - name: employee
        description: "Employee master — department, subsidiary, location attribution."
      - name: entity
        description: "Counterparty master — customers, vendors, employees, partners."
      - name: item
        description: "Product and service item master."
      - name: location
        description: "Physical and operational location master."
      - name: subsidiary
        description: "Legal entity hierarchy — IS_ELIMINATION flags intercompany netting entries."
      - name: transactionaccountingline
        description: >
          Core GL fact — one row per posted accounting line.
          POSTING=TRUE and VOID=FALSE filters applied in Silver.
          FX translation (REPORTING_AMOUNT_USD) pre-computed in Silver.
      - name: transactionstatus
        description: "Transaction status lookup — status codes decoded to human-readable labels."
